export type PageType = {
    label: string;
    path: string;
};

export type KeyWord = {
    id: string;
    text: string;
};

export type MessageContent = {
    text: string;
};

export type GroupType = {
    type: string;
    uid: string;
};

export type Bot = {
    uui: string;
    cookie: string;
};
